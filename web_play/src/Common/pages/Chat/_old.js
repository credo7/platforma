// const messages = chatRoom.messages?.nodes?.map((message) => ({
//   ...message,
//   users: message.users.nodes,
// }));

// let chat = {
//   room: chatRoom,
//   messages,
//   // usernames: participants
//   //   ?.map(({ user }) => user?.username)
//   //   ?.filter((item) => item.user?.username !== user?.username)
//   //   ?.join(", "),
//   // unread: item.chat.messages.totalCount,
// };
